node_modules/
*.log

# Transient captures from the harnesses. The curated set in shots/ is committed; the
# hundreds of frames tests/play.mjs and tests/browser.mjs produce are not.
shots/play-*.png
shots/browser-*.png
shots/mobile-*.png
shots/scene-*.png
shots/bigtext.png
shots/bundle-*.png
shots/sprites-probe.png

# The one-file build is generated: `node tools/bundle.mjs`, and it is a megabyte of
# duplicated source that would churn on every commit.
dist/
